/**
 * @kb-labs/analytics-cli
 * CLI commands are registered via cli.manifest.ts for @kb-labs/cli.
 * Use commands via: `kb analytics <command>`
 */

// Re-export manifest for compatibility
export { commands } from './cli.manifest';

